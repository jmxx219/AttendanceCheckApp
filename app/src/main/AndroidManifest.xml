<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancecheckapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceCheckApp">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Home">
        </activity>
        <activity
            android:name=".LectureListActivity"
            android:label="LectureList">
        </activity>
        <activity
            android:name=".StudentAttendanceActivity"
            android:label="StudentAttendance">
        </activity>
        <activity
            android:name=".ProfessorAttendanceActivity"
            android:label="ProfessorAttendance">
        </activity>
        <activity
            android:name=".ManualCheckActivity"
            android:label="ManualAttendance">
        </activity>
        <activity
            android:name=".AutoCheckActivity"
            android:label="AutoCheckActivity">
        </activity>
    </application>

</manifest>